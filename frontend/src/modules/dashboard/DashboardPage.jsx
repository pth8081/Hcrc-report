// modules/dashboard/DashboardPage.jsx — Khung trống, chưa gắn biểu đồ/KPI cụ
// thể — chờ yêu cầu chi tiết (số liệu nào, nhóm theo gì) trước khi xây thật.
export default function DashboardPage() {
  return (
    <div className="page">
      <h1>Dashboard</h1>
      <p>Chưa có nội dung — chờ yêu cầu cụ thể về KPI/biểu đồ cần hiển thị.</p>
    </div>
  );
}
