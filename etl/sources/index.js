// sources/index.js — Danh sách nguồn đang được đồng bộ. Thêm một nguồn mới:
// tạo file theo mẫu sources/_template.js rồi require() vào mảng dưới đây.
// KHÔNG import _template.js vào đây — nó chỉ là mẫu để copy, không phải nguồn thật.
module.exports = [
  // require('./ten-nguon-that'),
];
